package model.tiles;
import model.enumerators.*;;

/**
 * the LandSlideTile represents a tile of type landslide
 */
public class LandSlideTile extends Tile{

    /**
     * Constructor
     * creates a new tile of type landslide
     * Postconditions the new landslide has been created
     */
    public LandSlideTile(){
        super(TileType.LANDSLIDE);
    }
}
